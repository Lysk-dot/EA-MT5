# Database connection (aponte para o servidor Linux quando disponível)
DATABASE_URL=postgresql+psycopg2://ea:ea123@localhost:5432/ea

# API Token
ALLOWED_TOKEN=changeme

# Forward endpoints (configure quando o servidor Linux estiver pronto)
FORWARD_INGEST_URL=
FORWARD_TICK_URL=
FORWARD_TOKEN=
FORWARD_CONFIRM_URL=
